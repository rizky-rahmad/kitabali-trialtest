# Frontend environment — copy to ".env.local"
# Public base URL of the backend API (the Cloudflare Tunnel URL, or your VPS domain)
VITE_API_URL=https://driver-homes-admit-tractor.trycloudflare.com 
# VITE_API_URL=http://localhost:4000